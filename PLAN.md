# PLAN — 青简输入法 / Qingjian IME

> 项目章程与架构决策记录（ADR）。后续所有代码都须遵守本文约定。
>
> **名称**：中文正式名 **青简输入法**；英文正式名 **Qingjian IME**；简称 **青简** / **Qingjian**。
> 产品标题、界面与面向中文用户的文档优先写「青简输入法」；
> 代码、crate、仓库与路径一律用 `qingjian`。
> **「Qingjian」连写、首字母大写**——不写 `Qing Jian`，也不写成 `QingJian`。
> 项目沿革：**青简前身为 Stele-IME**（English: *Qingjian was formerly developed
> under the name Stele*）。旧名「Stele」只在沿革与致谢里出现。
> 版本 **v2** · WSL2 本地开发。v1 → v2 的变更记录见文末附录。
> 引擎抽象与数据结构的**权威定义**在 [`docs/engine-design.md`](docs/engine-design.md)，本文只记录决策与边界。

---

## 0. 定位

一个**独立的、从原理出发、用 Rust 实现**的输入法项目。借鉴 RIME 的设计思想与雾凇拼音（rime-ice）的部分数据/行为经验，但不以逐行重写 librime 或直接兼容全部 RIME 方案为目标。近期先服务作者自己的 Windows 全键盘拼音使用；Android 与九键是以后另立的产品路线。

### 0.1 动机（为什么做这个）

1. 现有商业闭源输入法**占用巨大**（实测峰值 300–400 MB）、功能冗余、隐私保护不完善。
2. RIME 是目前唯一可用的替代，但**依赖重**（Boost + LevelDB + marisa + OpenCC + yaml-cpp + glog）、构建复杂，且关键行为大量寄生在 Lua 插件上。
3. 因此：**做一个更小、更快、更可配置的独立 Rust 引擎**，而不是又一个 RIME 前端，也不是把 librime 原样搬到 Rust。RIME 是重要参考系，不是本项目必须完整复刻的产品规格。
4. **第三根支柱：离线与隐私的明确承诺。**
   - 经查证，**RIME 的官方设计文档中找不到任何关于隐私、离线或数据本地化的承诺**（三份独立对比报告一致确认 NOT FOUND）；作者 2009 年的开发计划第三期出现「**添加網絡功能**」。
   - **但这不能反推"librime 必不保护隐私"**：librime 本身是 BSD-3-Clause，"是否联网"取决于具体前端、部署、插件与用户配置，需逐项审计（审计 §3.1）。以上只是"上游文档没把隐私写成承诺"这一**事实**。
   - 所以"离线 + 隐私"**不是 RIME 的既有卖点，而是 Qingjian 自己可以立起来、并且必须自己兑现的旗帜**（§5.5 铁律）。
   - **不记录按键日志、不做遥测、不发网络请求**——这三条要写进面向用户的承诺里，而不只是内部约定；边界与"什么只是合同、什么已被代码核实"见 `docs/privacy-model.md`。

### 0.2 硬指标（不可退让的红线）

> 这两条是项目的存在理由，任何设计取舍都必须先过这一关。

| 指标 | 目标 | RIME 实测基线 |
| --- | --- | --- |
| **打字时常驻内存**（单方案） | < 30 MB | 约 20 MB（Squirrel） |
| **部署峰值内存** | < 150 MB | 780 MB – 1 GB |
| **按键延迟 P50** | < 1 ms | 0.48 ms（重度 Lua 方案） |
| **按键延迟 P99** | < 10 ms | 36.4 ms（重度 Lua 方案） |
| **冷启动到可打字** | < 100 ms | 未公开 |
| **编译产物 / 源 YAML 体积比** | < 3× | 约 14×（891 KB → 12.4 MB） |

**这些是青简的工程目标，不是“必须整体不比 RIME 差”的兼容承诺。**当前首先验证作者自己的 Windows 使用闭环；具体性能目标仍需按真实输入负载测量，不能用短输入单项数字代表整体体验。
这些数字目前是**目标而非事实**，必须在 P1 结束时用真实测量校正（见 §9 称重台）。

---

## 1. 决策记录（Decision Log）

| ID | 决策 | 理由 | 状态 |
| --- | --- | --- | --- |
| D1 | 项目代号 `qingjian` | 延续既有设计语言「碑铭」 | 已定 |
| D2 | 许可证 **MIT OR Apache-2.0** | Rust 生态惯例；成立的前提是 D10（不分发第三方数据） | 已定 |
| D3 | **Rust 原生引擎为主干** | 更快、更省、内存安全、跨平台 | 已定 |
| D4 | librime **仅作参考**，不链接其代码 | 学原理；按需把模块翻译成 Rust | 已定 |
| D5 | ~~词库数据用雾凇拼音的 `.dict.yaml`~~ | 见 D10 / D11：**格式兼容，数据自建或用户自备** | **已修订** |
| D6 | **选择性参考 RIME / 雾凇的关键行为** | 保留作者需要的输入习惯；只在青简选择复现某项行为时做对照。`others/no_lua_schema` 与上游方案不是当前硬性兼容目标 | **已修订** |
| D7 | ~~AI = 本地轻量 embedding，只做偏好重排~~ | 方向保留，**优先级下调**：见 D19 | **已修订** |
| D8 | 开发环境 **WSL2 Ubuntu**，仓库放 ext4 | 最快的内核迭代循环 | 已定 |
| D9 | 核心 crate **零第三方依赖优先** | 供应链安全、可审计。**限定为 `qingjian-core` / `qingjian-engine`**；周边 crate 允许受审依赖 | **已修订** |
| D10 | **第三方的原始词典数据与模型不随项目分发**；由 MIT/Apache-2.0 来源**生成**的默认词库随仓库分发（逐项见 `THIRD_PARTY_NOTICES.md`） | rime-ice 为 GPL-3.0-only，且内部词源含限制性条款；本地取回与编译属个人使用，分发才产生义务。注意：`schemes/qingjian-default/cn_dicts/generated.dict.yaml` 是已跟踪的派生产物，**不适用**"什么都未分发"的说法 | 已定 |
| D11 | 词典**格式**沿用 `.dict.yaml` 兼容写法，**数据自建/后置** | 格式是我们要长期支持的接口；自建数据可让权重可解释、无许可风险 | 已定 |
| D12 | 引擎抽象拆为 **`Engine`（共享）+ `Session`（私有）** | 共享昂贵的词库，隔离廉价的会话状态；同时获得编译期的线程安全保证 | 已定 |
| D13 | 候选分数使用**对数域** | 概率连乘变连加，避免下溢；重排器可直接加减贡献 | **已定：对数域 + 定点整数 `Score(i32)`** |
| D14 | **变体拼写（简拼/模糊音/补全）不进词库** | 规则的声明属于方案数据，编译进棱镜，运行时由通用切分器在图上求最短路；词库保持精确匹配 | 已定 |
| D15 | 候选排序必须是**全序** | 平局时依赖容器顺序会让结果不确定，违反 §5.1 | 已定 |
| D16 | **不内嵌脚本引擎**（Lua / JS 等） | 安全（输入法可见全部按键）＋ 行为可静态分析 ＋ 减依赖 | 已定 |
| D17 | 采用**组件注册表 + 零件自描述配置** | 零件可插拔、可裁剪（直接服务内存红线）；配置错误可被显式报出 | 已定 |
| D18 | 内存与速度是**硬指标**，P0 建"称重台" | 没有测量的目标等于没有目标 | 已定 |
| D19 | 记忆优先级：**频率+衰减 → 上下文(n-gram) → 向量** | 前两步收益大、代价小；向量收益不确定且直接冲击内存红线 | 已定 |
| D20 | **引擎通用性边界：引擎内不含任何输入法专属知识** | 引擎只认识"编码 / 拼写 / 编码单元 / 字母表"；拼音的音节表、拼音规则、词表**全部属于方案数据**。以"同一引擎 + 仓颉方案可打字"作为验收 | 已定 |
| D21 | 前端顺序：**先 Windows 自用闭环；Android/九键另立路线** | 近期目标是作者自己的 Windows 全键盘输入；手机尤其九键有独立的按键歧义、触摸交互、生命周期和专用方案问题，不能视为桌面路线的顺延 | **已修订** |
| D22 | 文档强度：**public item 英文强制；中文详解仅对非直觉 API / 算法 / 取舍强制** | 双语全覆盖在 P2 之后会成为主要摩擦 | 已定 |
| D23 | **候选分两条通道：`Lane::Input`（对应当前输入）/ `Lane::Predict`（对下一词的猜测）** | 让"精确优先"与"下一词预测"同时成立：铁律只约束 Input 通道，预测走独立通道、可混排、可一键关闭、不参与盲选 | 已定 |
| D24 | **内核与方案严格分离；雾凇只是"默认方案"的**行为**参照，不是内核的一部分** | 内核只提供机制（编码/拼写/候选/扩展点分类/服务接口）；一切输入法个性属于方案数据。默认方案是**我们自有的方案资产**（行为对标雾凇，数据自建），随项目分发但**与内核解耦** | 已定 |
| D25 | **方案采用分层组合 + 按条目 id 补丁 + `--dump-config`** | 借鉴 DSH；按位置补丁（RIME 的 `@before 0`）会随基准方案演进而静默失效。目标：**默认方案的任何一行都能被用户可靠覆盖** | 已定 |
| D26 | **配置错误绝不阻止启动：降级 + 保留上一份可用配置 + 可读诊断** | 输入法的失败是**自锁**的——拒绝启动会让用户连"打字去改配置"都做不到。DSH 的 `assertEntriesActivated`（一个坏插件中止整个启动）是开发工具的合理选择，**不是输入法的** | 已定 |
| D27 | **方案格式版本 + 零件 API 版本，两级门禁** | DSH 恰恰**没有**这个（`engines.dsh` 只声明、无人读取），它可以如此是因为插件与宿主同版发布；而**方案是用户写的长期资产**，格式就是对外契约 | 已定 |
| D28 | **编译产物按内容寻址 + 校验不通过即拒绝** | 防止"用错版本的产物凑合跑"；多版本可共存、可原子替换 | 已定 |
| D30 | **重写风险审计的四处修正**：`Engine` 与 `SchemaCatalog` 分层、`Session::switch_schema`、`Session::tick` 预留、公开枚举一律 `#[non_exhaustive]` | 每一项都属于"事后改就是 API 级重写"的类型。详见 `docs/engine-design.md` §14.2 | 已定 |
| D31 | **项目命名**：中文正式名「**青简输入法**」、英文正式名 `Qingjian IME`、简称「青简」/ `Qingjian`；代码与路径用 `qingjian`，产品标题与中文文档用「青简输入法」 | 「青简」本身即完整、统一的专有名词，**不意译为 Bamboo Slips 一类的英文词**：保留原语言名称既点明中文根源，也避免意译把文化意象压扁成普通英文词。拼写一律连写、首字母大写（**不写 `Qing Jian`，不写 `QingJian`**）。旧名 `Stele` 不消失，仅保留在项目沿革与致谢中（“青简前身为 Stele-IME”）。**取代原 D31**：旧名取自 RIME 主题名的碑刻意象，与「青简」无关，其"刻经于石"的解释一并作废 | 已定 |
| D32 | **语言形态：当前维护简体；繁体保留接口与配置，暂不承诺适配** | 开发者的书写习惯和测试经验均限于现行规范简体；为避免对不熟悉的文字形态作出未经验证的承诺，① 随项目提供的方案数据、测试和体验验收只覆盖简体；② `simplifier` 一类的转换**组件与开关一律保留**（接口不封死）；③ 欢迎后续基于这些接口扩展繁体支持，但在有相应维护者和可验证测试前，项目不承诺维护繁体数据或体验。 | 已定 |
| D33 | **P1 的验收同时覆盖两个方案：一个拼音方案 + 一个精确编码方案** | P1 必须有某个具体方案才能端到端验证管线，而使用者真实用的是拼音，故拼音方案是**用例**而非**前提**。但若只验证拼音，D20 的"引擎通用性"就仍只是承诺——**故从 P1 起就并跑一个精确编码方案**（无拼写运算、无切分图），让通用性从第一天起被测试保护 | 已定 |
| D34 | **标签只准从 `TagTable` 拿** | `Tag` 是 `&'static str`，而方案里的标签名在装载期 intern 一次。两处各自 `Box::leak("punct")` 会得到**两块内存**，`Vec::contains` 随之静默失效——标点整条链断掉且没有报错（真发生过） | 已定 |
| D35 | **配置值的单位写进字段名** | 边长用 `weight:`（线性比）或 `cost:`（毫对数），不共用一个字段。`cost: -3000` 被当成"权重为负"→ 掉到下界 → 切分退化成"谁先找到算谁"（真发生过） | 已定 |
| D36 | **`send` 从链头重派发 + 重入标志** | 照抄 librime：`ProcessKey` 是顶层入口，防重入靠 `redirecting` 布尔。我第一版"从 key_binder 之后 + 轮数上限"能跑，但换来的键在前面那些处理器眼里等于没发生过（源码证伪） | 已定 |
| D37 | **候选的第三个轴 `CandidateKind`** | `Origin`（从哪来）/ `SpellingAttr`（编码怎么拼的）/ `kind`（**怎么被找出来的**）。`autocap` 要判"是不是补全"、`reduce_english` 要判"是不是用户词"——用 `Origin` 都表达不了。**判据是"有没有零件真的按它分支"**，不是"RIME 有这字段" | 已定 |
| D38 | **外部不确定性一律注入** | 时钟 `Clock`（含 `utc_offset_secs`）、随机 `RandomSource`、记忆 `MemoryStore` 都是 trait，构造时注入。零依赖 + 可复现。**默认时区偏移是 0（按 UTC 报时）**——接前端时容易漏 | 已定 |
| D39 | **没有权威标准的行为，拿上游当 oracle** | `tools/oracle/`：上游函数的纯计算副本 + `luajit` 跑出的输出存档 + 逐字节比对。**"与上游一致"必须能被重新跑一遍**，否则它只是又一句没有证据的话。它抓出的 11 处错误全是"我以为理所当然" | 已定 |
| D40 | **"比上游更宽松"也是一种不一致** | 移植时要把**两侧的边界都对齐**，不只是"能跑"。`--3`（Lua 里 `--` 是注释）、`1+2)`、`sin(1,2)`（Lua 忽略多余实参）我第一版都比 Lua 宽松，后果是"上游说这个输入错了"变成"我们算了个数" | 已定 |
| D41 | ~~用户记忆的键是"规范化拼写"~~ | **当初的理由**：记忆挂在 `Ranker` 上，而重排器只看得到 `QueryView::input`（用户敲的那串），看不到编码。 | **已修订 → D42** |
| D42 | **用户记忆的键是"规范编码"，由翻译器随候选带出** | 编码**本来就在翻译器手里**（`Expansion::code`），是到了候选列表这一层才被丢掉的。把它渲染成键挂在 [`Candidate::key`] 上，于是：① 上屏时键随候选进入 `Commit`，落库不需要任何反查；② 重排时按候选自带的键查表，**同一条编码只有一把键**——`nhao` 与 `nihao` 走到 `[ni,hao]` 时拿到同一把 `ni'hao`，**跨拼法共享是自动的**。用户界面上与 rime-ice 等价（RIME 的用户词典同样以 `code` 为键） | 已定 |

> **D41 → D42 是一次自我纠正**：D41 把"重排器拿不到编码"当成了架构约束，
> 而它其实只是"当时的实现没有把编码传下来"。所有者追问"为什么不能用编码"
> 之后查明：`Candidate` 加一个字段就够了，不需要动内核的分层，
> 也不需要让记忆层去反查（反查才是当初被否掉的那条路）。
> **教训记在 HANDOFF §5 第 37 条**。
| D43 | **管线的排序在滤镜之前；`finalize` 已删除** | 滤镜用**位置**表达意图（`long_word_filter` 的 `idx: 4`），所以它看到的必须是最终顺序，**后面不能再排一次**。原先的顺序是"滤镜 → 重排 → 排序"，那次排序把 4 个重排型滤镜的效果**原样抹掉**（实测），而装配报告一切正常——本项目最恨的形状。删掉 `finalize` 而不是留成空操作，是为了不让人往里放"收尾排序" | 已定 |
| D29 | **方案可在会话中切换；多方案按内存预算惰性装载与淘汰** | RIME 作者原话即把"**有能力动态地调整组件的调度，以在会话中切换到不同的输入方式**"列为对 Engine 的两大期望之一。但方案含词库、**常驻内存可能翻数倍**，故必须：按需装载 + LRU 淘汰 + 遵守 §0.2 预算 | 已定 |
| D44 | **预测通道的编号与插入位置**（P4b） | 预测候选插在**第 1 个输入候选之后**（可配置），但**数字键数的是"输入候选的名次"**——换算只在 `SessionState::selectable_index` 一处完成 | `docs/engine-design.md` §4.3.2 要求预测插在第 1 名之后，而 §4.3.3 又要求"索引指向已渲染列表"：两条同时成立时，插进来的预测会把**后面所有输入候选的编号推后**，于是用户按 `2` 命中的是一条预测——而预测不参与盲选，表现为"按了没反应"。这正好破坏 §4.3.1 要保护的肌肉记忆（设计文档写下这两条时没注意到它们的交点）。**修法不是放弃插入位置，而是把两个编号空间显式换算一次** | 已定 |
| D45 | **预测的上下文 = trigram 优先 + bigram 回退**（P4b） | 一次上屏写**两条**预测记录（最近两个词 / 最近一个词）；查询时"长的有记录就只答它，没有才退到短的" | 所有者选了 **trigram** 粒度，但个人数据稀疏：只用 trigram 时新用户前几周几乎什么都预测不出来。选"**回退**"而不是"插值"，是因为插值要为两段上下文各定一套权重——那正是"配置数字的量纲"这类问题的温床；回退只有一条能被测试钉死的语义 | 已定 |
| D46 | **默认关闭的可选能力可以突破 30 MB 红线，但必须同时满足三条** | ① **默认关**，开启是一次**显式动作**；② 开启时的上限**写进文档、并在称重台报告里打出来**；③ **红线本身不动**——"我们比 RIME 小"这句话对**默认配置**仍然成立 | PLAN §0.2 的"常驻 < 30 MB"是这个项目的**存在理由之一**（RIME 实测约 20 MB）。但把所有可选记忆类能力都压在 30 MB 以内，会让"想用的人用不上"与"不想要的人被拖累"同时发生。折中是**作用域**：默认配置守红线，**显式开启**的能力单独记账。理由与 D26 同类——**放宽一条红线必须留下痕迹**，否则下一个人会以为 30 MB 从来就不是红线。（本条由所有者 2026-09 批准；首个适用者是 P5 的本地向量记忆） | 已定 |
| D47 | **近期目标是 Windows 全键盘自用闭环，不是完整 RIME 重写** | 项目首先要让作者自己在电脑上可用；范围先收敛到一个固定拼音方案、一个词库、最薄 TSF 前端。Android、九键、全量 RIME schema 兼容、终局算法优化均后置；RIME 只作选择性参考 | 已定 |

**关于 D5 / D7 / D9 的修订说明**

- **D5 → D10/D11**：v1 计划直接使用雾凇词库作为数据源。经核实 rime-ice 为 **GPL-3.0**，且其内部词源混合了 MIT / Apache-2.0 / LGPL / CC BY-SA，其中英文库（google-10000-english）明确限定"教育及个人研究用途"，腾讯词向量无许可声明。因此：**格式兼容，数据自建；是否引入外部数据推迟到引擎验证之后**。
- **D7 → D19**：向量重排仍是方向，但排在频率与上下文之后，且必须通过内存预算评审。
- **D9**：`qingjian-core` / `qingjian-engine` 保持零依赖；`qingjian-dict`（YAML 解析）、`qingjian-embed`（推理运行时）允许使用经过审查的依赖。
  **一处对计划的修正（P4a 实测之后）**：`qingjian-memory` 原本写的是 SQLite，实际做成了**自写紧凑 KV**——我们需要的只是一个"按键时零 I/O、启动时读一次"的有序表，那正是 `qingjian-table` 已经解决过的形状，于是它连依赖都不需要。这也让 `scripts/verify-deps.sh` 的白名单**至今是空的**。第 0 步先把那条门禁补上了（PLAN §4.8 的欠账），所以将来真引入依赖时，那一步无法被跳过。

---

## 2. 架构

### 2.1 分层

~~~
+--------------------------------------------------------------+
| 前端 Frontends    Windows TSF / Android IME / CLI(调试)        |
+--------------------------------------------------------------+
| 桥接 FFI          qingjian-ffi   (C ABI / JNI)                   |
+--------------------------------------------------------------+
| 会话 Session      每客户端一份，私有、可变（见 D12）             |
+--------------------------------------------------------------+
| 引擎 Engine       加载好的方案 + 词库，共享、昂贵、只读           |
+--------------------------------------------------------------+
| 抽象 Core         qingjian-core：trait 定义与数据结构              |
+--------------------------------------------------------------+

        侧挂服务（通过 trait 注入引擎，不是"上层"）
        +-------------------------------+  +----------------------+
        | qingjian-memory  Lexicon/Ranker  |  | qingjian-embed  Ranker  |
        +-------------------------------+  +----------------------+
~~~

> **v1 的图错在哪**：v1 把 `qingjian-memory` / `qingjian-embed` 画在引擎上方，但数据流又把"记忆与向量重排"放在引擎管线内部——两者不能同时成立。按 §2.5「外部资源一律 trait 注入」，它们是**注入引擎的服务实现**，不是上层。
> **注意**：上图的"层"是逻辑分层；实现上 `qingjian-memory` 依赖 `qingjian-core` 的 trait，**不依赖** `qingjian-engine`。

### 2.2 crate 结构

~~~
qingjian/
├── crates/
│   ├── qingjian-core/     # 抽象：Engine/Session trait、组件 trait、数据结构、错误
│   ├── qingjian-engine/   # 原生引擎：拼写代数、切分、翻译、过滤、重排管线
│   ├── qingjian-dict/     # 词典：.dict.yaml 解析 + 编译产物读写
│   ├── qingjian-config/   # 配置/方案模型：YAML 加载与校验
│   ├── qingjian-table/    # 词库编译产物：紧凑二进制 + 按需分页
│   ├── qingjian-schemes/  # 方案装载 + 内嵌默认方案（与内核分属不同 crate，见 D24）
│   ├── qingjian-memory/   # 用户记忆：频率、衰减（**自写紧凑 KV，零第三方依赖**）
│   ├── qingjian-embed/    # 轻量 embedding：条件性，见 D19
│   ├── qingjian-cli/      # 命令行调试前端
│   ├── qingjian-bench/    # 称重台：内存与延迟测量（P0 交付物，见 §9）
│   ├── qingjian-rime/     # 可选：librime 对照后端（仅测试，不做发布依赖）
│   └── qingjian-ffi/      # C ABI / JNI 桥
├── platforms/
│   ├── windows/        # TSF 组件
│   └── android/        # Kotlin InputMethodService + Rust .so
├── schemes/            # 方案资产（与内核解耦，见 D24）
│   └── qingjian-default/  # 默认方案：行为对标雾凇，数据自建；内核里没有它的任何痕迹
├── docs/
└── reference/          # 调研资料（RIME 内部机制、rime-ice、前端对接、官方 wiki）
~~~

### 2.3 核心抽象

引擎抽象、数据结构与组件 trait 的**完整定义见 [`docs/engine-design.md`](docs/engine-design.md)**。此处只列要点，避免两处定义漂移：

- `Engine: Send + Sync` —— 持有词库、方案、服务实现；构造昂贵，进程内共享一份。
- `Session` —— 每个客户端一份；所有变更走 `&mut self`，Rust 借此在编译期保证"一个会话同一时刻只有一个线程在用"。
- `Outcome { Rejected, Consumed, Committed(Commit) }` —— **上屏信息是返回值带出来的**，前端不可能像 RIME 的"读取即清空"那样漏读。
- `Candidate { text, comment, score(对数域), source, span, lane }` —— `source` 是"精确匹配优先"这条铁律的执行手段；`lane` 把"对应当前输入的候选"与"对下一词的猜测"分开（D23）。
- 组件 trait：`Processor` / `Segmentor` / `Translator` / `Filter`（骨架），`Lexicon` / `Ranker`（服务）。

### 2.4 数据流

`按键 → processor（是否消费）→ segmentor（拼写展开为编码，切分）→ translator（出候选）→ filter（纠错/简繁/标点/去重）→ ranker（记忆与向量重排）→ commit（上屏）`

> 与 RIME 内部分解一致；差异见 D12–D15。
> **注意**：以上每一步都是**通用机制**。拼音的音节表、简拼规则、词表全部是**方案数据**（D20）——引擎里不允许出现"音节""简拼"这类词，详见 `docs/engine-design.md` §2.4。

### 2.4.1 通用性边界（D20）

引擎提供**两族翻译器**，由方案自己选：

| 翻译器族 | 做什么 | 谁用 |
| --- | --- | --- |
| 精确编码翻译器 | 输入串当作完整编码，直接查表 | 仓颉、五笔、英文输入、自定义短语、拆字反查 |
| 拼写图翻译器 | 拼写展开成切分图，求最短路 + 组句 | 全拼、双拼、注音 |

**守住边界的检查**：`qingjian-core` / `qingjian-engine` 不得内置任何词表、音节表或拼音规则；P2 之后必须有一组"同一引擎 + 一份仓颉方案跑通编码→字"的测试。**这个测试写不出来，就说明边界没守住。**

### 2.5 边界规则

- 平台无关代码只放 `crates/*`；平台相关只放 `platforms/*` 与 `qingjian-ffi`。
- 引擎**不依赖任何 UI**，也**不直接读文件、时钟或网络**。
- 外部资源（存储、模型、时钟、随机数）一律通过 trait 注入，保证可测试（见 §5.5）。
- **禁止在引擎回调中重入引擎**（RIME 的回调是同步在调用线程上触发的，重入会导致死锁或借用冲突）。

### 2.6 组件模型（D17）

- 零件按**名字**注册在注册表里；方案（schema）只是**选择并配置**零件的声明式数据。
- 零件**自描述配置**：每个零件声明自己的参数名、类型、默认值；加载配置时一次性校验并给出可读错误（RIME 的静默忽略是反例）。
- 零件应当**可裁剪**：不使用的能力不应被编入二进制（直接服务内存红线）。
- 零件是**编译期/原生扩展**，不是运行时脚本沙箱（D16）。
- 保留"方案 / schema"这个面向用户的词——它是 Rime 生态的通用概念。

---

## 3. 路线图（Roadmap）

| 阶段 | 目标 | 主要交付物 | 验收标准 |
| --- | --- | --- | --- |
| **P0** | 环境、骨架、**称重台** | workspace + CI + 规范 + `qingjian-bench` | WSL2 里 `cargo build` / `cargo test` 通过；`qingjian-bench` 能输出内存与耗时 |
| **P1** | 原生内核最小闭环 | 切分（**含变体拼写**）+ 内存词库 + 候选 + 会话 + CLI + **两个方案** | ① 拼音方案：`qingjian nihao` → 「你好」，`qingjian nh` → 「你好」；② **精确编码方案**：`qingjian <code>` → 字（**同一引擎，通用性被测试保护**，D33）；单测覆盖；§0.2 指标首次实测 |
| **P2** | 词典格式与自建词库 | `.dict.yaml` 解析器 + 自建小词库 | 能加载自建词库并正常出候选 |
| **P2.5** | 词库编译器 | 流式编译 → 紧凑二进制 + mmap 加载 | 产物/源 < 3×；部署峰值 < 150 MB；`Lexicon` 实现可整体替换 |
| **P3** | 方案与组件能力 | 零件集 + 配置校验 + **分层补丁与 `--dump-config`**；选择性参考 RIME 行为 | 青简自己的方案行为有端到端测试；RIME 对照仅覆盖明确选择复现的行为，不要求直接加载上游 schema |
| **P3.5** ✅ | **默认方案（开箱即用）** | `schemes/qingjian-default`：自有方案 YAML + **41 万条**词库 + OpenCC 数据装载 | ✅ 装上就能打字（实测：`qingjian --scheme-dir schemes/qingjian-default nihao` → 你好）；**内核里没有任何它的痕迹**（门禁守） |
| **P4a** ✅ | 用户记忆：频率 + 时间衰减 | `qingjian-memory`（**自写紧凑 KV**，零依赖 + 内存缓存）+ 引擎侧 `Services` 注入 + CLI `--userdb` | ✅ 打过的词下次优先（端到端：同码两个词，选中 4 次后反超）；✅ **用户记忆的按键路径零磁盘 I/O**（`/proc/self/io` 的 `syscr`/`syscw` 与字节数在 1000 次按键后一个都没涨；**注意这是记忆路径的性质，词库查询 `TableLexicon` 仍用 `read_at`**）；✅ 重启后还在；✅ 内存增量实测 3 万条 +5.8 MiB（真实词库 + 记忆 = 17.8 MiB < 30 MB）；✅ 坏文件降级成"没有记忆"+ 一行警告（D26） |
| **P4b** ✅ | **本地下一词预测**（`Lane::Predict`） | `qingjian-memory` 的**预测表**（bigram + trigram，键 = **上下文**）+ 流水线插入 + CLI `--predict`（**默认关**） | ✅ 对比集 `tools/predict/collocations.tsv`（15 条常见搭配）**端到端全过**：学过之后期望词是预测通道的**第 1 位**（`cargo test -p qingjian-memory --test predict_next`）；✅ 内存增量实测约 **4.0 MiB**（默认 20 000 条 × ≈210 B/条）< 5 MB；✅ 按键延迟与无预测**无可测差异**（P50 约 48 µs / P99 约 104 µs，真实词库）；✅ **用户记忆与预测的按键路径零磁盘 I/O**（`/proc/self/io` 那条测试已扩展到预测；词库查询仍走 `read_at`）；神经模型**撞红线，暂缓**。执行书见 `docs/HANDOFF.md` §7.7 |
| **P5** | 向量偏好重排（**第一版已落地，默认关**） | `qingjian-embed` + 量化 | **可行性评审 + 第一版**（2026-09）：`docs/embed-design.md`（执行书与实测）、`docs/p5-vector-feasibility.md`（评审）。**本地、无模型、零依赖**：把用户本地历史里的 `(上下文 → 下一个词)` 计数投影成 `i16` 向量，在 `Lane::Input` 上加有界偏好分（前 3 名、≤4000 毫对数）。**默认关闭**（D46 第①条）。实测：对比集 3/3（基线 1/3，无回归）、向量表 **2.44 MiB @ 4 万词**、装载峰值约 +9 MiB、按键延迟无可测变化。**收益证据仍然很小（3 条自造用例）**，下一步是所有者手写更大的对比集，并把两步共现/子词回退补上 |
| **P6** | **Windows 自用闭环** | 最薄 TSF 组件：键盘事件、预编辑、候选窗、空格/数字选择、提交、基础编辑与安全上下文 | 作者可在 Windows 上完成真实日常输入；密码框/不可编辑控件不抢输入；现有输入法可作为兜底 |
| **P7（后置，另立路线）** | Android 全键盘/九键输入法 | `InputMethodService` + JNI；九键专用方案、触摸候选、生命周期和隐私边界 | 另行立项和验收；不作为 P6 或当前内核的隐含完成条件 |
| **P8** | 设置与人性化 | 设置界面 | 关键功能一键可达 |
| *可选* | librime 兼容层 | `qingjian-rime` | 仅作行为对照，不做发布依赖 |

**说明**

- **P3 不等于 RIME 兼容**：rime-ice / librime 是设计和行为参考。青简可以选择性复现某项行为，但不把直接加载全部上游 schema 作为当前验收线。
- **近期前端顺序已决（D21/D47）：先 Windows 自用闭环，Android 与九键后置并另立路线。** Windows 全键盘是当前最短的真实使用反馈路径；手机九键不是桌面方案的简单延伸。
- **P3 必须额外通过"通用性验收"（D20）**：用同一个引擎 + 一份仓颉或五笔方案跑通"编码 → 字"。**这个测试写不出来，就说明引擎里混进了拼音知识。**
- P2.5 独立成阶段的原因：**没有它，P3 的性能无法达标**；但它对 P1/P2 无阻塞，故后置。

---

## 4. 注释规范（Commenting Convention）

> 总原则：**代码里同时服务两类读者** —— 中文读者要能读懂「它在干什么、为什么」，GitHub 读者要能用惯例英文快速定位。

### 4.1 语言分工

| 对象 | 语言 |
| --- | --- |
| 标识符、文件名、crate 名 | 英文 |
| 分支名、commit message | 英文（Conventional Commits） |
| 模块文档 `//!` | 中英双语 |
| 公开 API 文档 `///` | **英文必写**；中文详解**仅对非直觉 API / 算法 / 取舍必写**（D22） |
| 行内注释 `//` | 中文为主，术语保留英文 |
| `unsafe` / FFI | 英文（Rust 惯例） |

### 4.2 模块文件头模板

~~~~rust
//! # Segmenter
//!
//! 中文职责：把连续拼音串切成合法音节序列，是候选生成的第一步。
//! English role: split a continuous pinyin string into valid syllables.
//! 架构位置：qingjian-engine 管线 input -> segment -> translate -> filter。
~~~~

### 4.3 公开 API 文档模板

~~~~rust
/// Split a continuous pinyin string into the highest-scoring syllable sequence.
///
/// 把连续拼音切成得分最高的音节序列：用动态规划，评分来自音节频次表。
/// 例：「xian」会在「xian」与「xi'an」之间择优。
///
/// # Arguments / 参数
/// * input — 纯小写拼音串。
///
/// # Returns / 返回
/// 最优切分；空输入返回空向量。
///
/// # Errors
/// 遇到非拼音字符或无法切分时返回 `SegmentError`。
///
/// # Examples / 示例
/// 见 crates/qingjian-engine/tests/segment.rs。
~~~~

> **章节标题必须用英文规范名**（`# Arguments` / `# Returns` / `# Errors` / `# Examples`），
> 中文说明写在正文里。
> **原因**：`clippy::missing_errors_doc` 等 lint 只识别英文标题——写成
> `# Errors / 错误` 会被判定为"缺少 `# Errors` 章节"。这一条是 P0 实建时被
> CI 报错逼出来的修正，不是审美选择。

### 4.4 行内注释

- 只解释**为什么**，不复述代码**做了什么**（命名本身就说明了 what）。
- 复杂算法必须写清思路与复杂度。
- 提供「反直觉取舍」的上下文，避免后人误改。

### 4.5 unsafe / FFI

- 每个 `unsafe` 块前必须有 `// SAFETY:`（英文），说明其成立前提。

### 4.6 commit / 分支

- Conventional Commits，英文，祈使句，主题不超过 72 字符；正文可中英混排。
- 示例：`feat(engine): add DP pinyin segmenter`、`fix(cli): handle empty input`。
- 分支：`feat/segmenter`、`fix/cli-empty-input`。

### 4.7 测试

- 测试函数名用英文描述行为：`segments_ambiguous_xian`。
- 每个 public API 至少一个正例 + 一个边界例。

### 4.8 工具强制（v2 新增）

- `rustfmt.toml`：统一格式；CI 检查 `cargo fmt --check`。
- Clippy：CI 上 `cargo clippy -- -D warnings`；`qingjian-core` 强制 `missing_docs`。
- `rust-toolchain.toml`：固定 toolchain 版本（见 §7）。
- CI 矩阵：至少覆盖 `linux`，并对 `windows` / `android` 目标做 `cargo check`。
- **依赖许可审查（v2 补）**：D9 允许周边 crate 使用"受审依赖"，但**"受审"必须有机制**——CI 上加 `cargo-deny`（或 `cargo-license`），对新增依赖做许可证白名单与重复依赖检查。否则 D9 只是一句自我声明。

> **已决（D22）**：v1 要求"每个 public item 都有双语文档"。因 P2 之后公开类型数量会快速增长，双语全覆盖会成为主要摩擦，故放宽为：**所有 public item 必须有简洁英文文档；中文详解仅对非直觉 API / 算法 / 取舍强制。** §4.3 的模板是"需要写中文详解"时的格式，不是每个 API 的最低要求。

---

## 5. 工程铁律（Invariants）

> **v1 的"确定性优先"是一条被过度概括的原则**：它把三件不同的事（可复现 / 候选封闭 / 精确优先）捆在一句座右铭里，而座右铭无法被证伪，可以被无限推广，最后挡住好功能（例如下一词预测）。v2 起拆成三条**各自带明确作用域**的规则（D23）。**能写成断言或测试的才是铁律；写不成的只能算倾向。**

1. **内存与延迟是硬指标**：见 §0.2。新增任何常驻数据结构都必须说明其内存增量；移动端如需放宽延迟必须先评审并记录理由。
   - 理由：这是项目存在的理由（§0.1），因此排在第一位。
   - **推论：按键路径上不许有事件总线。** 扩展点在**加载时装配**成静态流水线，按键时按固定顺序直接调用；禁止运行期事件广播、字符串查表、遍历注册项。
2. **可复现**：候选列表是 **(输入, 状态) 的纯函数**——同一状态、同一输入，永远给逐字节相同的候选序列。
   - 具体化：排序必须是**全序**（D15）；平局以 `Source` 优先级、再以**确定的插入顺序**裁决。
   - **不限制自适应**：自适应改变的是"状态"，不是破坏可复现性。
   - 必须写成测试：同一输入连跑 100 次，结果逐字节一致。
3. **候选封闭**：候选文本必须来自**可枚举的来源**（词库 / 用户词库 / 短语表 / 预测表 / 原样输入）。
   - **禁止自由文本生成。**
   - **理由（v1 从未说明，但这是本条最强理由）**：输入法是特权组件——它看得见全部按键，也能向任何输入框写字。若候选文本可由模型自由生成，就等于开了一条**数据外泄通道**。
   - 这条**允许** n-gram 预测、向量重排等一切"从已知集合里选"的技术。
4. **精确优先**：精确匹配不得被"猜出来"的候选压过。
   - **作用域限定**：只约束 `Lane::Input`（对应当前输入的候选），**不约束** `Lane::Predict`（对下一词的猜测）——见 D23 与 `docs/engine-design.md` §4.3。
   - 必须写成**代码里的断言**，而不是只写在文档里。
5. **离线**：引擎与记忆层零网络调用。
6. **可测试**：平台无关代码能纯 `cargo test`，不依赖 GUI 与系统。
7. **可替换**：外部资源通过 trait 注入。
8. **文档**：每个 crate 有 README；公开 API 按 §4 注释。
9. **零件可裁剪**：不使用的功能不应被编入二进制（D17）。
10. **通用性**：`qingjian-core` / `qingjian-engine` 内不得出现输入法专属知识（D20）。命名检查 + 依赖检查 + 仓颉方案测试。
11. **内核与方案分离**：内核提供机制，方案提供个性（D24）。**判定规则：数据能描述的选择属于方案；需要新控制流的属于内核。** 内核的边界是诚实的——加第五类扩展点、加服务接口、加改写运算，都要改内核。
12. **能力闭环（设计清单，不是闸门）**：
    - 新增一个能力时，**顺手**回答三个角色：**接口是什么（定义）/ 谁实现（提供者）/ 谁消费（消费者）**。
    - 答不出来的那个角色，**记进 §6 待决问题，并在代码里标 `TODO(seam)`**。
    - ⚠️ **它不是闸门**：**允许先把实验写出来**，只是不允许把"说不清"的东西当成"设计完成"。区别在于——闸门问"你能不能动工"，清单只问"你说清楚了吗"。
    - 对照：v1 的"确定性优先"之所以变成绊脚石，是因为它**没有写作用域**，于是可以被无限推广。**本节所有铁律都要求写明作用域与例外**，这条本身就是那个教训的产物。
    - 参考来源：DSH 的 seam 三角色（`docs/architecture.md`）。

---

## 6. 待决问题（Open Questions）

> **G 系列**是逐行对比 RIME 官方设计文档后发现的设计/兼容性参考项，详见 `docs/engine-design.md` §13。它们不是“必须先完整复刻 RIME 才能写代码”的闸门：近期按 D47 只处理阻塞 Windows 自用闭环的项；其余保留为长期候选。

- **`Candidate` 需要显式的插入序号 `seq: u32`**（P0 写测试时发现）：目前"平局按插入顺序"依赖**排序是稳定的**这一隐含性质，而守卫的精确检查也因缺少身份而只能退化成 O(n²)。加上 `seq`（由 `CandidateSink::push` 递增分配）后，① 守卫变 O(n)，② 插入顺序从实现细节变成显式数据。**在它落地前，精确守卫只能用于 debug 构建。**
- **G1 跨文件节点复用**（RIME 的 `__include`）：方案之间共享配置块的唯一干净手段；rime-ice 大量使用。**方案层必须支持"引用另一个文件的节点"**，求值顺序照 RIME 的固定规则（include → 合并同级字面值 → patch 子节点）。
- **G2 拼写层与词典层必须分开**：RIME 允许"用全拼的词库 + 双拼的拼写规则"。现在 `Lexicon` 同时含 `lookup()` 与 `alphabet()`，**把两个可独立替换的东西焊死了**。必须拆出拼写层（拼写 → 编码）。
- **G3 `Translator` 缺 tag 绑定**：tag 是"切分器 → 翻译器"的绑定层。需加 `accepts(&[Tag])`（`Filter` 已有 `applies_to`）。
- **G4 原样上屏恒存在**：必须有 echo/fallback 兜底——**你敲的东西永远能上屏**。
- **G9 `Source` 是有损建模**：RIME 的拼写属性（模糊/缩略/纠错/补全）是**可叠加的位集**，而我们的 `Source` 是单选题。必须拆成 `SpellingAttr`（位集）+ `Origin`（单值）。
- ~~**G10 用户词主键必须是规范编码**~~ **✅ P4a 已处置（D42）**：RIME 警告——用简拼/异拼/拼错的编码存进用户词典，会变成**永远检索不到的无效数据**，症状是"学过的词有时出现有时不出现"，极难排查。
  **处置与上游一致**：键就是**规范编码**。区别只在"谁来算"——RIME 在翻译器里查用户词典，我们把编码渲染成键**挂在候选上**，一路带到 `Commit` 与 `Event`。于是没有任何一层需要反查，而"写进去的键一定查得回来"是恒等式。
  **跨拼法共享因此也回来了**：用户用 `nhao` 学的词，敲 `nihao` 时照样优先（端到端断言 + CLI 演示都在 HANDOFF §4）。这条不变式进了 `qingjian --check` 的第 8 组。
- **G11 `MemoryStore` 缺 `forget`**：用户需要能**取消一次学习**（RIME：从词库删词只取消调频效果）。
- **G12 猜测候选要在 UI 上有标记**：RIME 对生成型候选打 ☯ 并提示用户确认。这既是安全阀，也让"精确优先"对用户可见。
- **G13 并击/时序类输入法**（RIME 的 `chord_composer`）：需要按键聚合与"单键 vs 和弦"的时序判定。我们的 `Key` 假设了字符键，**和弦不是字符**。P1 不做，但接口要留门。
- **G14 拼写运算的语义细节**：每个算子只跑一次不做不动点迭代；`erase` 是全匹配、其余是全局替换；`xlit` 唯一按 UTF-32 处理；分隔符是单个 ASCII 字符且参数内不能转义；**算子顺序即语义**。
- **方案切换（G2 的连带问题）**：`SchemaCatalog` + 会话内切换的具体 API；多方案的内存预算与 LRU 淘汰策略（D29）。
- **配置数值的域**：RIME 的 `initial_quality: 1.2` 是加到**线性计数**上的（`exp(weight) + initial_quality`），而我们的 `score` 是定点对数域。方案配置里的数字按哪个域解释、在何处换算，必须先定。
  **P4a 的部分答案**：用户记忆的加成**只在更新时换算一次、存下来的永远是定点 `Score`**，而且用的是**全整数**的曲线（`qingjian-memory/src/decay.rs`），所以连"更新时用浮点"都不需要。方案里其它配置数字（`initial_quality` 一类）仍未定。
- ~~**简拼的收益/成本**~~ **✅ P4a 已有实测数据**：拼音 / 真实词库下**每次按键的词典查询次数 P50 19、P99 64、max 64（平均 33.2）**。用 `qingjian-bench --scheme-dir schemes/qingjian-default --schema=pinyin --count-queries` 复现。**读法**：这是变体拼写的**成本**——每多一条拼写展开就多一次查表；而"收益"（少敲几个键）是使用者自己能感觉到的。
- ~~用户词库的并发模型~~ **✅ P4a 已定并实测**：`MemoryStore` 的方法都是 `&self`，实现里用 `RwLock`（查走读锁、写走写锁），中毒一律 `into_inner` 继续（D26：输入法的失败是自锁的）。**实测**：挂上 3 万条记忆之后按键 P50 只涨 1–2 µs，因此**不需要**"每会话一份只读快照"那类无锁读法（HANDOFF §7.6.2 第 3 步留的退路没有动用）。
- 声调：rime-ice 全库无调（`nve` / `lve` / `ju` / `qu` 写法），是否需要预留？
- 零件自描述配置的具体格式；简繁转换数据来源；模糊音/纠错数据来源。
- ~~向量模型的选型、量化与内存预算（P5 门槛）~~ **✅ 已研究（2026-09）**：见 `docs/p5-vector-feasibility.md`。结论是**三道闸**——① 内存：全词库 64 维 int8 要 **25.3 MiB**（实测），而留给 P5 的余量约 **6.4 MiB**；② 许可：能拿到的中文词向量是 CC-BY-SA（fastText）或授权不明（腾讯），都不可随项目分发；③ 收益：**必须先证明**它比 P4a+P4b 强（否则是重复品）。唯一同时过内存与架构的形态是**用户自备向量 + 文件背书（不进 RSS）+ 按词条号寻址**。复现工具 `tools/embed-probe/`。
- Windows TSF 的具体实现路径（见 `reference/rime-frontend-research.md`）：这是 D47 的近期工作，需要先确定最薄前端与 `Session` 的桥接契约。
- Android（尤其九键）的具体实现路径：后置并另立阶段；它不属于 Windows 全键盘自用闭环的隐含范围。

---

## 7. 本地环境清单（WSL2 Ubuntu）

**当前实际状态（已核对）**

| 项 | 状态 |
| --- | --- |
| 系统 | WSL2，内核 `6.18.33.2-microsoft-standard-WSL2`，x86_64 |
| 仓库路径 | `/home/brennmond/projects/qingjian`（ext4 ✅，**不要**放 `/mnt/c/...`） |
| git | 2.53.0 已装，**已 `git init`**，且有完整提交历史（首发 `3ff32a6`） |
| Rust | rustc / cargo **1.98.1，来自 Homebrew**（`/home/linuxbrew`）；**rustup 已装**（`~/.cargo/bin/rustup`，toolchain `1.98` 已装），但 PATH 上 `cargo` 仍先命中 Homebrew（`~/.cargo/bin` 在其后）——`rust-toolchain.toml` 只对 rustup 的 shim 生效 |
| 磁盘占用 | 已跟踪内容 **14 M**（含 41 万条生成词库）；`.git` 约 82 M，`target/` 可再生、不计 |

**待办（P0）**：无——三项均已落地（rustup 已装、`rust-toolchain.toml` 已写、`git init` 已有提交历史），实建记录见 §8「P0 已完成（实建记录）」。

> **背景（P6/P7 仍需）**：`rust-toolchain.toml` 的版本固定与 `rustup target add` 都依赖 rustup。P6/P7 需要 `x86_64-pc-windows-msvc` 与 Android NDK 目标，届时会卡住。

**后续（P6/P7 再备）**：Windows 侧 VS2022 + ATL/MFC + Windows SDK；Android 侧 Android Studio + NDK + CMake（Trime / fcitx5-android 均未使用 `cargo-ndk`，我们可行但缺少先例）。

---

## 8. 最近一步（Next Action）

### P0 已完成（实建记录）

| 交付物 | 状态 |
| --- | --- |
| workspace（`qingjian-core` / `qingjian-cli` / `qingjian-bench`） | ✅ |
| `cargo build` / `cargo test` / `cargo fmt --check` / `cargo clippy` | ✅ 全绿（35 单测 + 7 集成测试 + 2 文档测试） |
| `qingjian-core` 全部核心类型与 trait（按 `docs/engine-design.md`） | ✅ |
| 称重台 `qingjian-bench`（延迟分位数 / RSS / 启动，支持 `--json`） | ✅ 已跑出第一组基线 |
| CLI `qingjian`（`--check` 内核自检 / `--dump-config` 明确报未实现） | ✅ |
| 三条 CI 门禁脚本 | ✅ **并已反向验证能抓住故意违规**（P4a 增至四条：多了依赖许可审查） |
| `rust-toolchain.toml` / `rustfmt.toml` / `.gitignore` / 双许可证 / README / CI workflow | ✅ |

**P0 过程中发现并修掉的问题**（记录在案，因为它们说明"写代码"确实能发现"写文档"发现不了的东西）：

1. **`Score::saturating_add` 只防了 `i32` 溢出，没钳在值域内** —— 两个 `CEIL` 相加会得到 `42948`，一个超出定义域的分数，会静默压过一切。
2. **"重排器加成有上界 ⇒ 倒置不可能"是错的** —— 上界只限制移动幅度；差距小于上界时倒置仍可能。已修正文档与 trait 文档，并明确"守卫不是可选项"。
3. **"平局按插入顺序"隐含依赖排序稳定性**，且守卫因缺少身份而无法做到 O(n) —— 需给 `Candidate` 加显式序号 `seq`（见 §6 待办）。
4. **`#[derive(Eq)]` 与 `f32` 不兼容** —— 而记忆条目的衰减值本就不该用浮点（它影响候选顺序，属同一类可复现性问题）。已改为定点。

### P1 已完成（实建记录）

**验收标准全部达成**（`PLAN.md` §3 的 P1 行）：

| 验收项 | 结果 |
| --- | --- |
| 拼音方案：`qingjian nihao` → 「你好」 | ✅ |
| 拼音方案：`qingjian nh` → 「你好」（变体拼写） | ✅ 且分数更低、属性标为 `ABBREV` |
| **精确编码方案**：`qingjian --schema shape-demo ab` → 「十」 | ✅ **同一个引擎**（D33 的通用性验收） |
| 测试覆盖 | ✅ **99 个**（含 16 个方案级端到端集成测试） |
| §0.2 指标首次实测 | ✅ 见下表 |

**第一组真实数字**（release，20 万次迭代）：

| 指标 | 实测 | 目标 | |
| --- | --- | --- | --- |
| 按键路径 P50 | **241 ns** | < 1 ms | ✓ |
| 按键路径 P99 | **351 ns** | < 10 ms | ✓ |
| 内核排序 200 候选 P50 | 240 ns | — | |
| 常驻内存 | **3 MiB** | < 30 MB | ✓ |
| 引擎装载（两方案） | 32 µs | — | |

> ⚠️ **这些数字只反映管线本身的开销**：演示词库只有几十条词，词库查找几乎不花时间。
> 真实词库（几十万条）的成本要到 P2.5 才测得到。**不要把 241 ns 当成最终成绩。**

**P1 过程中发现的问题**（每一个都是"写代码才能发现"的类型）：

1. **`Rule::Identity` 是设计错误。** 我把"规范拼写"做成了一个需要显式声明的规则，
   于是"只写一条缩写规则"会导致**连规范拼写都查不到**。RIME 的代数里
   `Sa = (A → A)` 是**基线**，规则只在其上派生。已改为"规范拼写永远在内"。
2. **CI 门禁抓住了我自己的架构违规。** `verify-no-ime-vocab.sh` 报出
   `crates/qingjian-engine/src/builtin.rs` 里有 `pinyin_scheme` ——
   **方案数据被放进了内核 crate**，正是 D24 要禁止的。已抽出独立 crate
   `qingjian-schemes-builtin`。**这条门禁第一次运行就证明了自己有用。**
3. **`Event::Learned` 缺 `attr`。** 没有它，G10（落库前把编码规范化）**无法实现**——
   接收方无从知道该不该换算。已补。
4. **组件 trait 必须 `Send`。** 流水线整体要能被搬到别的线程（TSF 组件跑在
   别人的进程里），而组件被流水线按值拥有。
5. **`#[non_exhaustive]` 按设计生效**：`ProcessResult` 加了这个标注后，
   下游 `match` 立刻要求补 `_` 分支——**把"将来加变体"的成本提前暴露，
   而不是留到运行期静默走错分支**。
6. **测量尺子太粗。** 称重台最初用微秒，release 下所有数字都显示 `0µs`。
   **一把读数恒为 0 的尺子等于没有尺子**——已改为纳秒。

### P2 已完成（实建记录）

**验收标准达成**：`qingjian --scheme-dir <目录>` **能加载自建的方案与词库并正常出候选**，
且**不需要重新编译**。

```bash
$ qingjian --scheme-dir /tmp/myscheme --list
demo           我的方案                       family=-
$ qingjian --scheme-dir /tmp/myscheme mami
猫咪
$ qingjian --scheme-dir /tmp/myscheme mm      # 缩写规则同样生效
猫咪
```

**新增 crate**

| crate | 职责 | 依赖 |
| --- | --- | --- |
| `qingjian-config` | YAML 子集解析、`$ref` 跨文件引用、分层补丁、可读诊断 | 仅 `qingjian-core` |
| `qingjian-dict` | `.dict.yaml`（YAML 头部 + TSV 正文 + `import_tables`） | `qingjian-config` |
| `qingjian-schemes` | 方案装载；内嵌默认方案；目录装载 | 三者 + `qingjian-engine` |

**新增数据资产**：`schemes/qingjian-default/`（`pinyin` / `shape` 两份方案 + 三份词库）。
它们通过 `include_str!` **内嵌**，因此"文件是真身、二进制里是同一份"——
**单一数据来源**；而且 `qingjian` 每次启动走的都是**真正的解析器**，
不是一条"只有测试才会走"的旁路。**旁路从来不坏，也从来不证明什么。**

**P2 过程中发现的问题**

1. **装载器的响亮报错抓住了我自己的数据错误。** 词库里用了 `hua`，
   但方案的 `alphabet` 里没有它——装载期直接报出
   「词条引用了字母表里没有的编码单元「hua」」。
   若在运行期静默跳过，症状会是"有的词永远打不出来"，极难排查。
   **这条检查第一次真正运行就证明了价值。**
2. **`SchemeDef` 原本用 `&'static str` 存词条与标签**，而方案现在是运行时读来的。
   已改为拥有 `String`。**这正是"当时不改、以后就是重写"的那类改动。**
3. **手写 YAML 子集解析器是对的**，理由具体到三条：RIME 的节点级指令
   （`__include` / `__patch`）**在 serde 的数据模型里没有节点身份**；
   我们需要行号来做诊断；map 的书写顺序参与语义。
4. **`--scheme-dir` 的值一度被当成按键序列打了出来**——我自己写的参数解析 bug，
   **单元测试全绿也没发现，是"真的跑一遍"抓到的**。
   教训：功能必须端到端验证，"能编译 + 测试绿"不等于"能用"。
5. 不支持的语法（锚点、别名、标签、制表符缩进）**一律明确报错并解释原因**，
   而不是猜一个。被静默当成字符串的 `*alias` 会变成最难查的一类问题。

### P2.5 已完成（实建记录）

**这一阶段是补做的**：我在交付 P2 后直接写了"下一步 P3"，
**没有回头核对 §3 的路线图**——而 P2.5 就在那里。
这是流程疏漏，不是排期分歧。

**它为什么不能跳**（实测，不是估算）：

| 词条数 | 内存实现 | 编译产物（首次，含编译） | 编译产物（命中缓存） |
| --- | --- | --- | --- |
| 30（内嵌默认） | 3 MiB | — | — |
| **500,000** | **245 MiB** | **47 MiB** | **23 MiB** |
| 1,880,000（雾凇规模） | **约 0.9 GB（外推）** | 约 177 MB（外推） | 约 85 MB（外推） |

RIME 自己的部署峰值实测是 780 MB – 1 GB。
**不换实现，我们恰好复现了它最糟糕的那个问题——而"内存比 RIME 小"正是这个项目存在的理由。**

**新增 crate**：`qingjian-table`——紧凑二进制格式 + 流式编译器 + 按需分页的 `TableLexicon`。

**关于"mmap"的一处实现选择**：`std` 里没有 mmap，用它要引入 `memmap2`
（项目第一个第三方依赖）或写 `unsafe`。我们改用**索引常驻 + 词条按需
`read_at`**：内存效果相同（文件页留在内核页缓存，不计入本进程 RSS），
且**零依赖、无 unsafe**。一次查询两次系统调用（约 2 µs）。
**换回 mmap 是 crate 内部的一次改动**——因为 `Lexicon` 是 trait。

**`Lexicon` trait 的兑现时刻**：引擎**一行未改**。
`SchemeDef` 只多了 `DictSource::{Inline, External}` 两个分支，
而 `External` 是 `Arc<dyn Lexicon>`——**引擎始终不知道存储格式**。
"换成 mmap 实现，引擎不用改"这句话在此之前只是承诺，现在是被验证过的事实。

**P2.5 过程中发现的问题**

1. **我自己的接线 bug 让内存一度没有下降。** 部署路径先是照旧走了一遍内联装载
   （把 50 万条词全读进内存），**然后丢掉重编**。峰值自然不变。
   **教训：换实现时要确认旧路径真的没有被走到**——测试全绿也不会告诉你这件事，
   只有量出来的数字会。
2. **`code_count` 永远多 1**：前缀和数组多推了一个哨兵。单元测试抓到的。
3. **校验和的所有权要说清**：必须由调用方从**源文件字节**算出，
   写入器不能把词条内容混进去——否则"产物是否对应这份源"就无法从源独立重算，
   而那正是重新部署时唯一能做的判断。
4. **仍然超标的地方（诚实）**：外推的编译峰值约 177 MB 高于 150 MB 预算；
   命中缓存时的峰值里含一份**源文件的瞬时缓冲**（校验和要把源读一遍，
   雾凇是 46 MB）。改成"先比大小与 mtime，不一致才读内容"可以去掉它。
   这两条都还没做。

### P3 进行中（第一块：完整拼写代数）

**已交付：RIME 的运算子集合，含一个自写的正则引擎。**

| 运算子 | 语义 | 状态 |
| --- | --- | --- |
| `xlit` | 字符转写（UTF-32） | ✅ |
| `xform` | 正则改写（**原拼写失效**） | ✅ |
| `erase` | 全匹配消除 | ✅ |
| `derive` | 派生（**两者都有效**） | ✅ |
| `fuzz` | 派生 + `FUZZY` 属性 | ✅ |
| `abbrev` | 派生 + `ABBREV` 属性 | ✅ |

**真实 RIME 写法现在能直接解析**，端到端验证（`schemes/` 之外的自建方案）：

```
speller:
  algebra:
    - xform/^([nl])ue$/$1ve/
    - derive/^([zcs])h/$1/
    - abbrev/^([a-z]).+$/$1/
    - erase/^hm$/
```

| 敲 | 结果 | 说明 |
| --- | --- | --- |
| `nihao` | 你好 | 规范拼写 |
| `nh` | 你好（`attr=ABBREV`，标"猜的"） | 缩写 |
| `nve` | 虐 | `xform` 从 `nue` 派生出的拼写 |
| `nue` | 落兜底 | **`xform` 使原拼写失效——RIME 文档明说的语义** |
| `zang` | 张 | `derive` 从 `zhang` 派生 |
| `hm` | 落兜底 | `erase` 消除 |

**正则引擎是自写的**（`qingjian-engine/src/regex.rs`，16 个测试）：
支持字面量、`.`、字符类与范围、`\d\w\s`、捕获组、选择、`* + ? {n,m}`、锚点。
**不支持的语法（非贪婪、反向引用、环视）一律报错并指出替代写法**，
而不是"大概能跑"。

**两个被测试抓到的真错误**（都由我自己引入）：

1. **`Rule::equivalence` 用字符类 + 字面替换做不到逐字符映射**——
   `[zh]` 配替换串 `"zh"` 会把 `zhang` 变成 `zzhangh`。
   已改为**专门的变体**（逐字符映射 + 派生语义）。
2. **我一度把"编码"与"拼写"的关系搞反**：字典的键是**字母表条目**
   （规范编码），运算子只改变**用户能敲什么**。这正是 RIME 的「拼寫 ≠ 編碼」，
   而我自己在写测试数据时犯了这个错。

**P3 收尾（本次交付）**

| 项 | 交付 | 实测 |
| --- | --- | --- |
| **零件集** | `recognizer` / `matcher` / `affix_segmentor` / `punct_segmentor` / `ascii_composer` / `navigator` / `key_binder` / `punctuator` / `punct_translator` / `reverse_lookup_filter` / `simplifier` | 注册表 24 个名字：**已实现 22、缺数据 2、缺代码 0** |
| **词条补全** | `Lexicon::prefix_lookup`（默认空 = 不支持）+ `BTreeMap::range` 前缀区间扫描 | `nihao` 能出「你好世界」，且**排在精确匹配之后** |
| **`--dump-config` 完整版** | 分层（内置/方案/用户补丁）+ 每个值的**来源**（层、文件、行号）+ 可粘贴的覆盖片段 | `switches` 逐项标到第 17/20/24 行 |
| **分层补丁接线** | 用户补丁**真的进装载路径**了（`<schema_id>.custom.yaml`） | 端到端测试断言"补丁改的值真的进了引擎" |
| **与 librime 的对照** | `tools/compare-librime.py` + `tools/librime-probe/`（无 `librime-dev` 时用 `rime_get_api()` 函数表驱动） | **6 条结构用例全过**，报告见 `tools/librime-probe/samples/compare-report.md` |
| **按键语义对齐** | `send` 从链头重派发 + 重入标志、动作选择链、`set_option`/`unset_option`、12 个编辑器动作 + `noop`、`when: predicting` | 每条都有"只在正确实现下才通过"的测试 |

**零件覆盖报告的读法**（`qingjian --components`）：剩下的 2 个是
`simplifier@emoji` 与 `simplifier@traditionalize`，缺的是 **OpenCC 的数据文件**
（emoji.json / s2t.json），而机制已经实现并可用内联表验证——
`crates/qingjian-schemes/tests/schemes/p3features.schema.yaml` 的 `fanti` 段就是。

**"跑通 `no_lua_schema`"这句话现在的准确状态**：

- **零件层面**：24 个名字里的 22 个我们有实现，另 2 个的机制也有
  （只差数据文件）。缺的**具体数据**是：`emoji.json`、`s2t.json`、
  `melt_eng.dict.yaml`、`radical_pinyin` 词典、`custom_phrase.txt`。
- **配置层面**：RIME 的原生写法（`engine:` 名字列表、`recognizer.patterns`、
  `punctuator` 的两张表与符号表、`editor.bindings`、`key_binder.bindings`、
  `affix_segmentor` 的 `prefix`、`import_preset`）**都能读**，
  且有 `p3features` 那份方案的 19 条端到端断言守着。
- **诚实的一句**：我们**没有真的把那份文件跑起来**（拿不到它的词库与
  OpenCC 数据）。跑通的是"同一种形状 + 全部零件"。

### P3 过程中被源码证伪的四个实现（都由测试或调研抓到）

| # | 我写的 | 事实 | 抓它的是 |
| --- | --- | --- | --- |
| 1 | 流水线每轮重算识别结果，**没告诉切分器** | 切分器一直用构造时的空扫描 → 整条"识别→切分→绑定"链静默断掉 | `the_symbol_table_expands_under_its_prefix` |
| 2 | 标签 `punct` 有两处来源，一处没走 intern 表 | 同名两块内存 → `contains` 失效 → 标点整条链断 | 同上 |
| 3 | `cost` 一个字段两种单位（毫对数/线性权重） | 写 `-3000` 被当成"权重为负"→ 掉到下界 → 切分退化成"谁先找到算谁" | `the_input_is_cut_into_labelled_segments` |
| 4 | `prefix: "uU"` 读成"大小写二选一"；`send` 从 key_binder 之后重派发 | librime 是**字面串**；`send` 从**链头**重跑、防重入靠一个布尔（`affix_segmentor.cc` / `key_binder.cc`） | 源码调研（`reference/rime-*.md`） |

前三条的症状完全一样：**配置看起来正常、某个功能就是不生效、没有任何报错**。
第四条更值得记：我把它写进了**文档注释并称之为"RIME 约定"**——
猜出来的约定写进文档，比写在代码里更危险。

### 对照实验的一个直接收益

`tools/compare-librime.py` 跑第一次就发现：默认拼音方案**没有声明
`punctuator`**，于是 `,` 什么都打不出来，而 librime 出「，」。
修法不是特判，而是给默认方案补上 RIME 形状的 `engine:` 零件清单，
并给引擎加了**预设**机制（`import_preset`，见 `qingjian_engine::presets`）。

### 性能（改动之后实测，release）

| 指标 | P1 基线 | P3 之后 | 目标 |
| --- | --- | --- | --- |
| 按键路径 P50（拼音，零件齐全） | — | **601 ns** | < 1 ms ✅ |
| 按键路径 P99 | — | **1.56 µs** | < 10 ms ✅ |
| 按键路径 P50（字形码，零件少） | 301 ns | **180 ns** | — |
| 常驻内存 | 3 MiB | **4 MiB** | < 30 MB ✅ |

**一处值得记的数字**：加完零件后 P50 一度是 **1.55 µs**（5 倍回归）。
原因不是零件本身，而是流水线为构造 `Query` **每键克隆三份会话状态**——
而其中两份**全项目零个使用者**。删掉 `Query::composition` 之后回到 511 ns，
再加"没有认领时整串一段"的快速路径，落到 542 ns；
阶段 A 又加了 10 个内联零件之后是 601 ns。

`qingjian-bench` 因此多了 `--schema=`：**延迟数字必须注明测的是哪个方案**
（零件数差别很大，180 ns 与 601 ns 是同一套代码）。

### 阶段 A：把 rime-ice 的 Lua 插件重写成原生零件（10/10，已装配）

**为什么单列一节**：rime-ice 的默认方案里，品牌输入法"上手快"的那些功能
**全部住在 Lua 插件里**（上游 `engine:` 里 38 个零件中有 16 个是
`lua_processor` / `lua_translator` / `lua_filter`）。要接近那种体验，
就得把它们做成原生零件——而**引擎里不许有 Lua**（D9 零依赖 + D24）。

| 已实现（10） | 行为 |
| --- | --- |
| `date_translator` | `rq`/`sj`/`xq`/`dt`/`ts`/`rqzh`/`rqen` |
| `unicode_translator` | `U62fc` → 「拼」+ 同区后续码位 |
| `uuid_translator` | 触发词 → UUID(v4) |
| `number_translator` | `R3355` → 四种中文形态（含金额大写） |
| `calc_translator` | `cC1+2` → 3（自写表达式求值器） |
| `long_word_filter` | 长词优先 |
| `autocap_filter` | `HEllo` → `HELLO` |
| `v_filter` | v 模式单字优先 |
| `pin_cand_filter` | 置顶 + 简码派生 |
| `reduce_english_filter` | 英文候选降权 |

> **装配状态（2026-09 补齐）**：这 10 个零件**曾经只有实现、没有装配**——
> 注册表标着"已实现"，而 `LoadedSchema::build_pipeline` 里一次都没引用过它们
> （详见 `docs/HANDOFF.md` §5 第 36 条）。现在 **10/10 全部装配**，配置可读，
> 端到端断言在 `crates/qingjian-schemes/tests/inline_components.rs`。
> 判断"某个零件名有没有装配分支"的可执行答案是
> `qingjian_engine::scheme::assembles(name)`，且有一条测试守着它与实现不漂移。

**余下 4 个都已被归类（不是"没做完"）**：

| 零件 | 缺什么 | 归类 |
| --- | --- | --- |
| `corrector` | 容错表在**上游的词库里** | `NeedsResource` |
| `lunar` | 1900–2100 二进制表（上游单独发布） | `NeedsResource` |
| `select_character` | "候选能被当输入用"的**会话语义** | 阶段 C |
| 拆字辅码 `search` | 同上 + 一个反查索引 | 阶段 C |

### 方法：拿上游当 oracle（D39）

`number_translator` 与 `calc_translator` 的行为**没有权威标准可对照**
（一个是会计习惯，一个是上游自己的选择）。这种情况下"与上游一致"
是唯一诚实的判据，而它必须**能被重新跑一遍**：

```
tools/oracle/<零件>/
  ├─ *.lua            ← 上游函数的**纯计算**副本（剥掉 RUNTIME 接口）
  └─ *.expected.txt   ← 用 luajit 跑出来的实际输出，存档
```

测试逐字节比对，并断言**存档行数与用例数一致**（防"存档被删后测试永远通过"）。
它抓出的 11 处错误全是"我以为理所当然"：

| 零件 | 抓到的 |
| --- | --- |
| `number_translator` | 6 处——含**Lua 的 `gsub` 默认只替换第一处**（我按"全局替换"实现，因为那是名字给我的印象） |
| `calc_translator` | 5 处——含**三处我们比 Lua 更宽松**（`--3`、`1+2)`、多余实参），而"更宽松"也是一种不一致 |

**这三份 `.lua` 不参与构建、不进二进制**，是实验记录；
它们在 `tools/` 而不是 `crates/`，因为**内核 crate 不许有数据文件**（D24，
门禁抓到过我一次）。

### 下一步

当前不再把“完整 RIME 重写/兼容”作为下一步。近期执行目标按
[`docs/SELF_USE_WINDOWS_SCOPE.md`](docs/SELF_USE_WINDOWS_SCOPE.md) 收敛为：

1. **Windows 自用闭环**：实现最薄 TSF 前端，完成键盘事件、预编辑、候选、选择、提交和基础编辑；
2. 用作者自己的日常输入集进行真实验收，只修错词、错选、丢字、崩溃、卡顿和前端契约等实际阻塞；
3. 冻结 Android、九键、全量 RIME schema 兼容、向量继续扩展和非阻塞终局算法重构；
4. 保留现有可靠性、资源上界、内核测试和 CLI 回归门禁。

以下清单是长期候选项，不得在没有重新排序前自动开工：

1. ~~**P3.5 词库管线**~~ ✅ **已完成**：干净来源的词表（`tools/fetch-sources.sh`
   + `tools/wordlist-gen/`）编出 **41 万条**默认词库；OpenCC 数据装载打通
   （emoji / 简繁两条端到端断言）。**雾凇那 44 MB 词表仍不进仓库**——
   最大几块来源不明或明确限制；我们只取 MIT / Apache-2.0 的那 9 份，
   且源数据仍由使用者自己取回（生成产物随仓库分发）。
2. **选择性 RIME 对照**：工装已经就位（`--scheme-dir` + `compare-librime.py` + `tools/oracle/`）；只为青简明确选择复现的行为提供证据，不把 `no_lua_schema` 或全部上游 schema 兼容设为硬性目标。
3. **阶段 C：会话语义改造**（解锁 `select_character` + 拆字辅码），待 Windows 自用反馈证明它是阻塞项后再排序。
4. **`send_sequence` 与 `set_option` / `unset_option` / `select`**：
   调研列出而我们尚未实现的四个按键动作（`reference/rime-key-binding-actions.md`
   的第 6 节有完整差异表）。
5. **中缀识别**：`recognizer` 目前只认**输入开头**的模式
   （见 `docs/engine-design.md` 的说明与 `segmentor.rs` 的边界注释）。

---

## 9. 性能与内存预算（称重台）

**为什么单列一节**：§0.2 的红线没有测量手段就只是一句愿望，而 v1 的路线图里完全没有基准任务。

**P0 必须交付**

1. `qingjian-bench` 能测量并输出：
   - 按键延迟分布（P50 / P95 / P99 / max），输入序列可配置
   - 常驻内存（RSS）
   - 冷启动耗时
   - 编译产物体积与体积比
2. 一份**基线记录表**（记录本机硬件、日期、每次改动的数字），随代码一起维护。
3. 记录对手基线的方法（RIME 的数字来自公开实测，需注明出处，见 `reference/rime-frontend-research.md`）。

**基线记录表**（release，`--iterations=50000`；同一套代码因**零件数**不同而差异很大，
因此**每个数字都注明方案**）：

| 阶段 | 方案 / 词库 | 零件数 | P50 | P99 | 常驻内存 | 测试数 |
| --- | --- | --- | --- | --- | --- | --- |
| P1（首次实测） | 拼音（默认装配） / 演示 | 少 | **241 ns** | 351 ns | 3 MiB | 99 |
| P3 加完零件后 | 拼音（RIME 形状清单） / 演示 | 多 | **1.55 µs** | — | 4 MiB | — |
| P3 删掉 `Query::composition` 后 | 拼音 / 演示 | 多 | **531 ns** | 852 ns | 4 MiB | 282 |
| P3 收尾 | 拼音 / 字形码 / 演示 | 多 / 少 | **542 / 180 ns** | 851 ns | 4 MiB | 322 |
| 阶段 A 之后 | 拼音 / 演示 | 多 | **601 ns** | **1.56 µs** | **4 MiB** | **334** |
| **P3.5 之后** | **拼音 / 真实词库 41 万条** | 多 | **约 50 µs**（热页缓存约 15 µs） | **约 110 µs** | **13.6 MiB** | **360** |
| P3.5 之后（同一套代码） | 拼音 / 演示词库 | 多 | 601 ns | 1.56 µs | 4 MiB | 360 |
| **P4a：挂上 3 万条用户记忆** | **拼音 / 真实词库** | 多 | **约 50–52 µs** | **约 110 µs** | **17.8 MiB** | **441** |
| P4a：一次运行里新学 3 万条 | 拼音 / 真实词库 | 多 | 同上 | 同上 | 19.4 MiB | 441 |
| **P4b：挂上预测（2 万条上限，默认关→开）** | **拼音 / 真实词库** | 多 | **48.0 µs** | **104.3 µs** | **19–20 MiB**（见下方读法） | **467** |

> **P4b 那一行的读法**：延迟那一行是**同一台机器、同一份产物**下
> "无预测 48.06 / 104.26 µs" 与 "有记忆+预测 48.08 / 103.05 µs" 的对照——
> **差别落在噪声里**（预测查询是一次 `BTreeMap` 区间扫描）。
>
> **常驻内存的 19–20 MiB 是一个被低估的数**：引擎装载那一段会留下一大片
> 已驻留的空闲堆，前两万条记录直接把它填满，RSS 一动不动。权威的单条成本
> 只能用"突破默认上限量斜率"得到：
> `qingjian-bench --scheme-dir schemes/qingjian-default --schema=pinyin --userdb /tmp/m.mem --predict-cap=300000 --seed-predict=N`
> 给出 **≈188 字节/条（10 万条）/ ≈211 字节/条（20 万条）**，
> 因此默认上限 20 000 条 ≈ **4.0 MiB**（P4b 的验收是 < 5 MB）。
> 这个方法上的坑记在 HANDOFF §5 第 44 条。

> **记忆这一行的读法**：与"无记忆"那两行相比，P50/P99 的差别在 **1–2 µs**
> 之内（三次重复测量的区间：无记忆 48.6–50.0 µs，有记忆 49.2–51.1 µs），
> 常驻从 13.6 MiB 涨到 **17.8 MiB**——都在红线的三分之一以内。
> 上限取 **30 000 条**是实测反推的：计划里建议的 100 000 条要 +18.2 MiB，
> 叠上词库会**越过 30 MB**（见 `qingjian_memory::DEFAULT_CAPACITY` 的文档）。
>
> **每键词典查询次数**（PLAN §6 那条一直没答的问题）：拼音 / 真实词库下
> **P50 19、P99 64、max 64，平均 33.2**。用
> `qingjian-bench --scheme-dir … --schema=pinyin --count-queries` 复现。
> 这个数字是**变体拼写的成本**：每多一条拼写展开就多一次查表。

> **这两行的差距（601 ns vs 50 µs）全部来自词库大小**：演示词库几十条，
> 真实词库 41 万条。**不看词库就引用延迟数字，等于没有数字**——
> 称重台因此加了 `--scheme-dir`（与 `--schema=` 一起用）。
>
> 首次部署（编译 41 万条产物 + 装载）实测 **0.62 s**、峰值 **21 MiB**；
> 产物 14 MB、源 `.dict.yaml` 11 MB（**1.3×**，红线是 3×）。

> **1.55 µs 那一行值得留着**：加零件本身不是原因，流水线为构造 `Query`
> **每键克隆三份会话状态**才是（而其中两份全项目零个使用者）。
> 删掉之后回到 531 ns。**这就是"加字段前先问谁读它"的价值。**

**关键认知（来自实测）**

- **文件大 ≠ 内存大**：词典用 mmap 按需分页，真正危险的是**部署峰值内存**与**产物体积**，不是稳态常驻。
- **渲染可能比引擎慢两个数量级**：Weasel 仅把 Direct2D 渲染目标改为 SOFTWARE，首次输入延迟即从 260–820 ms 降到 42–98 ms（中位 73 ms），而引擎本身只要约 0.5 ms。**P6/P7 的候选窗渲染性能必须当作一等公民。**
- **用户记忆的按键路径零磁盘 I/O**：RIME 的 P99 36 ms 里，最大的单点瓶颈据称是 LevelDB 的磁盘读取。P4a 把用户词库全量放内存、异步批量落盘。
  **这不是"整条按键路径零 I/O"**：词库查询（`TableLexicon`）仍按需走 `read_at`，
  冷页缓存下每次查询两次 `read_at`（§9 与 HANDOFF §0 的读法）。审计 §0.2 明确反对把它写成绝对承诺。

---

## 10. 数据与许可策略

**已核实的事实**

| 对象 | 许可 | 影响 |
| --- | --- | --- |
| **librime** | BSD-3-Clause | 引擎可放心参考（D4）；`tools/librime-probe/probe.c` 逐字段抄了其 ABI 声明，按 BSD-3-Clause 保留声明（见 `licenses/BSD-3-Clause-librime.txt`） |
| **rime-ice 仓库** | **GPL-3.0-only** | 随项目分发其数据会触发传染。其 `opencc/emoji.*` 也在 GPL 之下（旧脚本误标为 Apache-2.0，已纠正）——本仓库**不分发**它们 |
| **Rime 官方 wiki** | **未声明**（UNVERIFIED） | `reference/wiki-*.md` 是逐字副本；许可状态未确定，见 `THIRD_PARTY_NOTICES.md` §5.3 |
| radical_pinyin（拆字） | GPL-3.0 | 不可随 MIT / Apache 项目分发 |
| THUOCL、pinyin-data | MIT | 可分发（需署名） |
| jieba（`extra_dict/dict.txt.big`） | MIT | 可分发（需署名） |
| rime-melt（melt_eng） | Apache-2.0 | 可分发（需署名） |
| rime-essay-simp | LGPL-3.0 | 不可随 MIT / Apache 项目分发 |
| google-10000-english（英文库） | **LDC 限制**：仅"教育及个人研究" | 商业用途需另行授权 |
| 腾讯词向量（tencent 词库） | 仓库内**无许可声明** | 不可分发 |
| 8105 字表 | 维基词典（CC BY-SA，含传染性） | 不可分发 |

**策略（D10 / D11）**

1. **引擎与编译器随项目分发；词典数据与模型不随项目分发。**
2. 用户若需要外部数据，**由用户在本地自行获取并编译**——本地编译属个人使用，不构成分发。
3. 引擎先用手写小词库验证；**是否引入外部数据推迟到 P1 / P2 完成之后**，届时按上表逐项评估。
4. **这个策略与性能目标方向一致**：RIME 本来就必须在部署期编译词典才能快，所以"本地编译"既是法律上最干净的做法，也是工程上最优的做法。

**实际分发的内容（阶段 4 / 审计 J2 核实，2026-09）**

上面讲的是"不分发什么"。**实际随仓库分发的第三方内容是这些**：

1. `schemes/qingjian-default/cn_dicts/generated.dict.yaml` —— 41 万条，
   由 pinyin-data / THUOCL / jieba（MIT）与 OpenCC `TSCharacters.txt`
   （Apache-2.0）生成；`pinyin.schema.yaml` 的 `speller.alphabet` 段同源。
2. `tools/librime-probe/probe.c` —— 含逐字段抄自 librime（BSD-3-Clause）的
   结构体 / 宏声明。
3. `reference/wiki-*.md` —— Rime 官方 wiki 两页的**逐字副本**，
   **许可状态未确定**（`THIRD_PARTY_NOTICES.md` §5.3 已标为待决）。
4. `tools/oracle/*/*.expected.txt`、`tools/librime-probe/samples/*` ——
   上游程序跑出来的**输出记录**（数据 / 事实，不是上游源代码）。

逐项的 artifact、上游 URL、**固定 revision**、版权、许可、是否修改与
许可文本位置，全部在根目录 [`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md)；
许可证文本在 `licenses/`；下载来源的固定 revision 与 sha256 在已跟踪的
`tools/sources.lock`。**三处必须同步更新。**

**P3.5 定下的三条边界**（与项目所有者共同确认，2026-09）

1. **Qingjian 保持 MIT / Apache-2.0**。内核、零件、默认方案的数据都是自有代码。
2. **不复制 rime-ice 的 Lua 实现**。它的**行为**（用户看到什么）可以对齐，
   **代码**不搬——因此插件全部按行为重写（阶段 A）。
   这也顺带避开一个更细的坑：那些脚本带着 Lua 的痕迹
   （`yield` 流式产出、`env` 全局表、字符串 `gsub`），逐行翻译会把
   痕迹搬进 Rust，而类型系统本来能做得更好。
   **阶段 4 补记（审计 J2.3）**：`tools/oracle/` 下曾有三份从 rime-ice
   复制/改写的 `.lua`（`calc.lua`、`number_to_chinese.lua`、`calc_probe.lua`），
   它们与这条边界冲突，**已移除**；只保留 `.expected.txt` 输出记录，
   上游 URL 与固定 revision 留在 `tools/oracle/README.md`。
3. **雾凇的词表不进仓库**，由用户部署时自取。理由不只是"GPL 要传染"：
   那 44 MB 里最大的两块（`tencent` 16.9 MB、`base` 16.2 MB）是
   **来源不明或明确限制**的，而"来源不明"比 GPL 更难处理——
   GPL 至少有一条明确的规则可循。

**一处应当讲清的常见误解**：把 GPL 插件与 MIT 内核**一起打包分发**，
那一整份分发就落入 GPL——GPL 不允许"把 GPL 部件放进宽松作品、
只给部件标 GPL"。所以"插件用 GPL、内核仍然 MIT"在**同一个安装包里**
是不成立的。成立的做法只有一条：**那个部分是独立的项目、独立分发**。

**GPL 传染的是"作品"，不是"事实"**：读上游的词表来理解
"雾凇会给出哪些候选"不受限；把它**打包分发**才受限。

---

## 附录：变更记录 v1 → v2

**新增决策**：D10 – D28。

**修订**
- **D5**：从"直接使用雾凇词库数据"改为"格式兼容 + 数据自建 / 用户自备"（许可原因，见 §10）。
- **D7**：向量的优先级下调到频率与上下文之后，并增加内存评审门槛（D19）。
- **D9**：零依赖范围限定为 `qingjian-core` / `qingjian-engine`。
- **D6**：复刻范围收敛为 `others/no_lua_schema`（无 Lua 的雾凇）。
- **D14**：从"简拼归属切分器"精化为"变体拼写的**声明**属于方案数据、**编译**属于通用编译器、**运行**属于通用切分器"；引擎内不得出现拼音词汇。

**修正的错误**
- §2.1 分层图与 §2.4 数据流的矛盾（记忆 / 向量被同时画成"上层"和"管线内"）。
- §2.3 `Engine` trait 的缺口：`commit` 丢失学习信息、缺少 `Session` 与 `Candidate` 定义、候选一次性全算。
- **§2.4 引擎里混进了拼音知识**（由项目所有者审阅时发现）：v2 初稿把"音节 / 音节表 / 简拼"当作引擎的通用模型，等于把拼音标准强加给所有方案，与"方案可自由设定"直接冲突。已修正为"编码单元 / 编码字母表"这套通用词汇，并把通用性边界升级为 **D20 + `docs/engine-design.md` §2.4**，附可执行的检查手段（命名检查、依赖检查、"同一引擎跑通仓颉方案"测试）。
- **§5.1「确定性优先」被过度概括**（由项目所有者审阅时提出）：v1 那条原则把**可复现 / 候选封闭 / 精确优先**三件事捆成一句座右铭，其中"绝不压过精确匹配"在 `docs/engine-design.md` §5.3 里被实现成了**禁止混排**的断言，比它自己声明的更严——会直接禁掉"下一词预测插在第 2 位"这类功能。已拆成三条带明确作用域的规则（§5），并把"内存与延迟"提为第 1 条。理由：座右铭无法证伪，会被无限推广，最后挡住好功能。
- §3 缺少词库编译器阶段（现为 P2.5）与基准任务（现为 P0 + §9）。
- §7 环境假设与实际不符（仓库路径、rustup 缺失）。

### 新增章节
- §0.1 动机、§0.2 硬指标、§2.6 组件模型、§4.8 工具强制、§9 称重台、§10 数据与许可策略。

### 第三轮修订（项目所有者指出"内核与方案边界不清"之后）

- 新增 **D24 内核与方案分离**、**D25 方案分层组合与按条目 id 补丁**。
- 新增 §5.11 / §5.12 两条铁律（内核与方案分离、能力闭环清单）。
- 路线图新增 **P3.5 默认方案**：雾凇只是**行为参照**，默认方案是**自有资产**（`schemes/qingjian-default/`），内核里不留它的痕迹。
- `docs/engine-design.md` 新增 §2.5（完整分界表 + "我要加个功能该放哪"决策地图）、§6.1–6.3（三层能力模型、按条目 id 补丁、注册可撤销）。
- **修正一处同类错误**：`Query` 里曾有一个 `lexicon` 字段——**那是"单个词库"的假设被焊进内核**，而雾凇一个方案挂了 4 个以上词库。已改为**服务在组件构造时注入**。
- **补齐一个缺失的扩展点类别**：`Formatter`（RIME 有 `formatter.h`，v2 初稿漏了它，导致"方案里怎么配置显示格式"在设计里无处安放）。

### 第四轮修订（读完 DSH 源码之后）

- 新增 **D26 配置错误绝不阻止启动**、**D27 方案与零件两级版本门禁**、**D28 编译产物按内容寻址**。
- **修正一处刚写错的策略**：上一轮把"加载期报错"写成了接近"拒绝启动"。**输入法的失败是自锁的**——拒绝启动会让用户连打字去改配置都做不到。已改为降级 + 保留上一份可用配置 + 可读诊断。
- §5.1 增加推论：**按键路径上不许有事件总线**（DSH 自己承认瀑布式分发"不如直接调用直接"，而它那边是每次工具调用走一次，我们是每次按键走一次）。
- `docs/engine-design.md` 新增：§5.7（热路径无事件总线）、§6.2.1（RIME `*.custom.yaml` 缺的四项：可撤销 / 可溯源 / 冲突检测 / 坏配置不致命）、§6.4（产物完整性与两级版本门禁）、§6.5（把原则变成 CI 脚本 + 自动生成方案字段目录）、§2.5.1b（"这是零件还是函数库"判据）、§5.3.1（**单调守卫**：光有 debug 断言，发布版里铁律无人保护）。
- §6.1b 记录**一处故意不抄 DSH 的地方**：它规定"行序不承载语义"，而我们的**滤镜顺序就是语义**（雾凇配置里明文写着 `pin_cand_filter > emoji > 简繁`）。**借它的抽象，不借它的假设。**
- 新增 `reference/dsh-plugin-architecture-lessons.md`。

### 第六轮修订（逐行对比 RIME 官方设计文档之后）

- 读了 RIME 作者的全部设计文档（25 页，存档于 `.rime-wiki/`），产出 4 份逐行引用的对比报告。
- **结论**：Qingjian 的 D20/D24 **不是新发明，而是 RIME 2009 年的原始设计**（作者原话：「輸入引擎是跨輸入法的通用程序，輸入方案／schema 即是那差異的部份」「爲了避免知道得太多，這引擎的內部構造必須精巧」）。librime 在十几年演进中偏离了它；**我们在把它拉回来**。
- **发现并补齐 14 个我方缺口（G1–G14）**：跨文件节点复用、拼写层与词库层分离、翻译器的 tag 绑定、原样上屏兜底、`Source` 有损建模、用户词主键必须是规范编码、取消学习、UI 猜测标记、并击时序预留、拼写运算语义细节等。详见 `docs/engine-design.md` §13–§14。
- **最有价值的三个发现**：
  1. **`Source` 是有损的**——RIME 的拼写属性（模糊/缩略）是**可叠加的位集**，单选题表达不了。已拆为 `Origin` + `SpellingAttr`。
  2. **用户词主键必须是规范编码**——否则会变成"永远检索不到的无效数据"，症状是"学过的词有时出现有时不出现"。
  3. **RIME 的官方文档没有把隐私与离线写成承诺**——文档 NOT FOUND，且作者早期计划里出现「添加網絡功能」。**这是 Qingjian 可以立自己旗帜的地方**，已提升为 §0.1 的第三根支柱。**但不能由此推断"Rime 不隐私/会联网"**：librime 是 BSD-3-Clause，是否联网取决于前端、部署、插件与配置（审计 §3.1）。
- **重写风险审计**（§14.2）又发现并修掉 4 个地基问题：`Engine` 与方案目录的角色冲突、缺 `switch_schema`、缺 `tick` 时序预留、公开枚举不可扩展。
- 新增 **D29 方案切换**、**D30 审计修正**、**D31 项目命名**。

### P0 实建（第七轮）

- 按 `docs/engine-design.md` 建起全部核心类型与 trait，`cargo build/test/clippy/fmt` 全绿。
- 三条 CI 门禁脚本（`scripts/verify-*.sh`，P4a 增至四条）**已反向验证**：故意插入违规能被抓住，
  而不是空转通过——**一个不会失败的检查等于没有检查**。
- 本轮发现 4 个实现层问题（详见 §8），其中 1 个推翻了文档里的一处论断。

### 新增文件
- `docs/engine-design.md`（引擎设计权威定义）
- `schemes/qingjian-default/`（默认方案资产，见 P3.5）
- `reference/`（调研资料：librime 内部机制、rime-ice、前端对接、RIME 官方 wiki 备份）

### 过程记录

v1 → v2 之间做了三轮一手调研（librime 源码与接口、rime-ice 配置与词库实测、Weasel / Trime 前端对接与性能实测），结论见 `reference/`。第三轮修订之前又读了 DSH（DeepSeek Harness）的架构文档，借鉴了它的 seam 三角色、分层组合与"我要加功能该放哪"决策地图（见 D24 / D25）。

**v1 中若干数字与推断在调研后被更正**，例如：filters 实为 11 个（非 13）；`rime_ice.schema.yaml` 无 `prism:` 键；rime-ice 全库拼音无声调；简拼（abbrev）默认开启而模糊音默认关闭。

**两个由项目所有者审阅发现、模型自查未能发现的错误**（记录在此以说明审阅的价值）：引擎里混进拼音知识（§2.4）、"确定性优先"被过度概括（§5.1）。两者的共同形态是——**把某个具体方案的假设，当成了通用于所有方案的真理**。

[package]
name = "stele-cli"
description = "Stele-IME 的命令行调试前端。"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
stele-core = { path = "../stele-core" }
stele-engine = { path = "../stele-engine" }
stele-memory = { path = "../stele-memory" }
stele-embed = { path = "../stele-embed" }
stele-schemes = { path = "../stele-schemes" }

# 可执行文件名与包名分开：用户敲的是 `stele`，不是 `stele-cli`。
[[bin]]
name = "stele"
path = "src/main.rs"

[lints]
workspace = true

[package]
name = "stele-table"
description = "词库编译产物：紧凑二进制格式、流式编译器、按需分页的 Lexicon 实现。"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# 零第三方依赖。这是刻意的：mmap 不在 std 里，用它要么引入 memmap2、
# 要么写 unsafe；而"索引常驻内存 + 词条按需 read_at"能达到同样的
# 内存效果，且**完全用 std**。见 crate 文档。
[dependencies]
stele-core = { path = "../stele-core" }

[lints]
workspace = true
